# 完整首轮任务

请从零实现“带捕获组的有限正则匹配引擎”，用于语言运行时 / 自动机中的本地可复用模块。

1. 从零实现正则编译和完整匹配库，语法限定为字面Unicode字符、转义元字符、点号（匹配任意单个code point）、连接、|、捕获括号、贪婪* + ?；不支持反向引用、前后查找、字符类或花括号量词。非法语法带位置报错。
2. compile生成带捕获起止标签的有序Thompson NFA；fullmatch要求消耗整个字符串。分支优先左侧，贪婪量词优先继续；同一路径重复捕获保留最后一次完成的捕获，未参与捕获返回None，空捕获返回空跨度。
3. 运行时使用有序状态集合，不使用指数回溯。明确并实现同一位置相同状态的优先路径保留与epsilon闭包访问规则，含可空子表达式的重复必须终止；捕获索引按code point。
4. 返回整体跨度及按括号开启顺序编号的捕获跨度。提供实际处理状态数，禁止调用re进行业务匹配或以输入样例硬编码；允许测试中对支持且语义相同的子集用re.fullmatch交叉验证。
5. 限制模式长度512、捕获组32、输入长度10000；测试灾难回溯形态的失败匹配并检查状态处理随输入近似线性增长，不用严格墙钟时间作为唯一证明。仅fullmatch，不扩展search/替换。

验收重点：嵌套捕获、左分支优先、(a|aa)*、可空重复、空输入、Unicode、未参与组与空组、缺闭括号、长失败串。

工程范围：这是全新核心库的0-1实现，初始仓库仅有运行骨架，没有业务实现。仅使用Python 3.14.7标准库，在Windows原生环境离线运行；无需安装第三方依赖。不接数据库、中间件、云API、外部程序，不使用Docker，不制作前后端或通用CLI。
交付：实现可复用库、聚焦上述语义的自动化测试和README；python demo.py须在约8秒内运行确定性小案例，打印关键正常结果和至少一种真实边界失败，便于录制十几秒视频。测试命令为python -m unittest discover -s tests -v。先阅读本仓库README和TASK.md，简短计划后集中实现，再运行针对性测试与演示；避免反复通读仓库、无关探索和无必要的长篇文档，发现实际问题时正常修复验证，不设硬性工具调用截断。
只能操作当前仓库内文件；不读取用户配置、密钥、环境变量清单或其他工作区。不要新增GitHub Actions、CI、部署、Dependabot或远程推送。一次首轮完成，不要求用户追加“继续”；合理实现取舍写入README。不要为通过测试删除需求、硬编码示例输出或宣称未实际执行的验证通过。
