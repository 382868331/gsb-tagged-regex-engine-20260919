# 完整首轮任务

实现一个小型正则匹配引擎，支持下列语法和捕获组。这里只做完整匹配，但分支优先级、重复和捕获结果都要确定；请从当前骨架开始写。

实现compile/fullmatch，语法仅Unicode字面字符、转义元字符、点号（含换行任意code point）、连接、|、捕获括号及贪婪* + ?。允许空模式和空括号；[]{}^$保留，须转义才作字面量。拒绝其他转义、空alternation分支、量词叠加、字符类及其他扩展。

编译时计算nullable，*或+的操作数可匹配空串则报错（如(a?)*、(a*)+）；?可作用于可空体。生成有序Thompson NFA与捕获标签；分支优先左侧、量词优先进入，fullmatch必须消耗整个输入。

有序状态集合每个输入位置对同一状态仅保留最高优先路径，epsilon闭包按优先序处理并去重，不使用指数回溯。返回按左括号顺序编号的code point捕获跨度；成功路径上每组最后一次完成的捕获保留，从未参与为None。

提供实际处理状态计数；业务匹配不得调用re，测试可用re.fullmatch(pattern,text,re.DOTALL)对共同支持子集交叉核验，未参与组和空跨度须区分。只实现fullmatch，不做search、替换或其他贪婪规则。

模式<=256字符、括号深度<=32、捕获组<=16、输入<=2000；用固定模式增加失败串长度观察状态计数随长度线性增长，不依赖墙钟基准。演示重叠分支捕获及可空重复被拒绝。

测试里请覆盖这些情况：左分支优先、(a|aa)*、(a(b)?)+保留已完成捕获、空组/未参与组、换行dot、可空重复拒绝、长失败串。

使用 Python 3.14.7 标准库，代码要能在 Windows 原生环境离线运行。范围就是这个库，不需要第三方依赖、外部服务、Docker 或前后端。请一并写好测试和简短的 README，说明接口及运行方法。

用 `python demo.py` 运行演示，在约 8 秒内展示一个正常结果和一个实际触发的失败；用 `python -m unittest discover -s tests -v` 运行测试。随机验证采用固定种子和少量小样本即可，不做长时间压力测试，也不扩展额外的平台功能。

请在本轮完成实现和验证，不要等我回复“继续”。正常排错需要的工具调用不设次数限制。操作范围限于当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区。不要添加 Actions、CI、部署或 Dependabot，也不要向远端推送。演示结果应由代码实际计算；最后如实说明验证结果，未运行或未通过的检查也请写明。
