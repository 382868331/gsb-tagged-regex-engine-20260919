# 完整首轮任务

协议校验组件需要无指数回溯风险的有限正则完整匹配，并返回捕获跨度。请从零实现“带捕获组的有限正则匹配引擎”。

1. 实现compile/fullmatch，语法仅Unicode字面字符、转义元字符、点号（含换行任意code point）、连接、|、捕获括号及贪婪* + ?。允许空模式和空括号；[]{}^$保留，须转义才作字面量。拒绝其他转义、空alternation分支、量词叠加、字符类及其他扩展。
2. 编译时计算nullable，*或+的操作数可匹配空串则报错（如(a?)*、(a*)+）；?可作用于可空体。生成有序Thompson NFA与捕获标签；分支优先左侧、量词优先进入，fullmatch必须消耗整个输入。
3. 有序状态集合每个输入位置对同一状态仅保留最高优先路径，epsilon闭包按优先序处理并去重，不使用指数回溯。返回按左括号顺序编号的code point捕获跨度；成功路径上每组最后一次完成的捕获保留，从未参与为None。
4. 提供实际处理状态计数；业务匹配不得调用re，测试可用re.fullmatch(pattern,text,re.DOTALL)对共同支持子集交叉核验，未参与组和空跨度须区分。只实现fullmatch，不做search、替换或其他贪婪规则。
5. 模式<=256字符、括号深度<=32、捕获组<=16、输入<=2000；用固定模式增加失败串长度观察状态计数随长度线性增长，不依赖墙钟基准。演示重叠分支捕获及可空重复被拒绝。

验收重点：左分支优先、(a|aa)*、(a(b)?)+保留已完成捕获、空组/未参与组、换行dot、可空重复拒绝、长失败串。

工程约束：全新0-1核心库，仅用Python 3.14.7标准库，在Windows原生离线运行，无第三方依赖、外部服务、Docker或前后端。交付实现、聚焦上述边界的测试及简短README；演示命令`python demo.py`用小案例在约8秒内打印正常结果和一种真实失败，测试命令`python -m unittest discover -s tests -v`。固定种子随机验证只用少量小样本，避免长压测和额外平台功能，不限制正常纠错所需工具调用。
一次首轮完成，不要求用户追加“继续”。只操作当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区；不添加Actions/CI/部署/Dependabot、不远程推送，不硬编码演示或虚报验证。
